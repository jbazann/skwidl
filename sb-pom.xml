<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.0</version>
        <relativePath/> <!-- lookup parent from repository -->
    </parent>
    <packaging>pom</packaging>
    <groupId>com.jbazann</groupId>
    <artifactId>tpdan-sb</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>tpdan-sb</name>
    <description>bombas de papa</description>
    <properties>
        <java.version>21</java.version>
        <spring-cloud.version>2023.0.2</spring-cloud.version>
        <jbazann.port>12012</jbazann.port>
        <jbazann.k8sProbePort>12112</jbazann.k8sProbePort>
        <jbazann.jarname>${project.artifactId}-${project.version}.jar</jbazann.jarname>
        <jbazann.dirname>jbazann</jbazann.dirname>
        <jbazann.volname>${jbazann.dirname}/mounted</jbazann.volname>
        <jbazann.replicaCount>3</jbazann.replicaCount>
    </properties>
    <modules>
        <module>config</module>
        <module>eureka</module>
        <module>customers</module>
        <module>orders</module>
        <module>products</module>
    </modules>
    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
    </dependencies>
    <profiles>
        <!-- Run mvn with '-P skipImage' to skip everything before and including Docker image build -->
        <profile>
            <id>skipImage</id>
            <build>
                <plugins><!--
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-plugin-plugin</artifactId>
                        <version>3.13.1</version>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>-->
                    <plugin>
                        <artifactId>maven-resources-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-compiler-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-surefire-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-jar-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <artifactId>maven-clean-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.springframework.boot</groupId>
                        <artifactId>spring-boot-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>repackage</id>
                                <goals>
                                    <goal>repackage</goal>
                                </goals>
                                <configuration>
                                    <skip>true</skip>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <artifactId>maven-install-plugin</artifactId>
                        <configuration>
                            <skip>true</skip>
                        </configuration>
                    </plugin>
                    <plugin>
                        <groupId>org.eclipse.jkube</groupId>
                        <artifactId>kubernetes-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>k8s-images</id>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                                <configuration>
                                    <skip>true</skip>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.springframework.retry/spring-retry -->
            <dependency>
                <groupId>org.springframework.retry</groupId>
                <artifactId>spring-retry</artifactId>
                <version>2.0.6</version>
            </dependency>
            <!-- https://mvnrepository.com/artifact/org.springframework.boot/spring-boot-starter-aop -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-aop</artifactId>
                <version>3.3.1</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <build>
        <resources>
            <resource>
                <directory>${project.basedir}/src/main/resources/</directory>
                <includes>
                    <include>*.properties</include>
                </includes>
                <filtering>true</filtering>
            </resource>
        </resources>
        <pluginManagement>
            <plugins>
                <!-- JKube images and deploy configuration -->
                <plugin>
                    <!-- https://mvnrepository.com/artifact/org.eclipse.jkube/kubernetes-maven-plugin -->
                    <groupId>org.eclipse.jkube</groupId>
                    <artifactId>kubernetes-maven-plugin</artifactId>
                    <version>1.16.2</version>
                    <executions>
                        <execution>
                            <id>k8s-images</id>
                            <phase>install</phase>
                            <goals>
                                <goal>build</goal>
                            </goals>
                            <configuration>
                                <images>
                                    <image>
                                        <!-- this is the default image name, see https://eclipse.dev/jkube/docs/kubernetes-maven-plugin/#image-name -->
                                        <name>%g/%a:%l</name>
                                        <build>
                                            <!-- plugin defaults COPY the final artifact to targetDir inside the image -->
                                            <from>openjdk:${java.version}</from>
                                            <assembly>
                                                <targetDir>/${jbazann.dirname}</targetDir>
                                            </assembly>
                                            <ports>
                                                <port>${jbazann.port}</port>
                                                <port>${jbazann.k8sProbePort}</port>
                                            </ports>
                                            <volumes>
                                                <volume>/${jbazann.volname}</volume>
                                            </volumes>
                                            <entryPoint>
                                                <shell>java -jar ${jbazann.dirname}/${jbazann.jarname} --spring.profiles.active=test</shell>
                                            </entryPoint>
                                        </build>
                                    </image>
                                </images>
                            </configuration>
                        </execution>
                        <execution>
                            <id>k8s-deploy</id>
                            <phase>deploy</phase>
                            <goals>
                                <goal>resource</goal>
                                <goal>apply</goal>
                            </goals>
                            <configuration>
                                <generator>
                                    <config>
                                        <spring-boot>
                                            <jolokiaPort>0</jolokiaPort>
                                            <prometheusPort>0</prometheusPort>
                                            <webPort>${jbazann.port}</webPort>
                                        </spring-boot>
                                    </config>
                                </generator>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- Exclude Lombok from final artifact -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>

                <!-- Skip install default behavior -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-install-plugin</artifactId>
                    <configuration>
                        <skip>true</skip>
                    </configuration>
                </plugin>

                <!-- Skip deploy default behavior -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-deploy-plugin</artifactId>
                    <configuration>
                        <skip>true</skip>
                    </configuration>
                </plugin>

                <!-- Delete original artifact to fix issues with k8s:build -->
                <!--<plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <executions>
                        <execution>
                            <id>delete original artifact</id>
                            <phase>package</phase>
                            <goals>
                                <goal>clean</goal>
                            </goals>
                            <configuration>
                                <excludeDefaultDirectories>true</excludeDefaultDirectories>
                                <filesets>
                                    <fileset>
                                        <directory>${project.build.directory}</directory>
                                        <includes>
                                            <include>*.jar.original</include>
                                        </includes>
                                    </fileset>
                                </filesets>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>-->
            </plugins>
        </pluginManagement>
    </build>
</project>
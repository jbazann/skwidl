apiVersion: v1
kind: Service
metadata:
  name: customers
spec:
  type: ClusterIP
  selector:
    app: customers
  ports:
    - name: app
      port: 12012
      targetPort: 12012
      appProtocol: https
    - name: probe
      port: 12112
      targetPort: 12112